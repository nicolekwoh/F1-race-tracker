[build]
  functions = "netlify/functions"

# Checks every 5 minutes for a GP that just finished (function itself decides
# whether to actually send — see weekly-race-email.mjs for the trigger logic).
[functions."weekly-race-email"]
  schedule = "*/5 * * * *"
